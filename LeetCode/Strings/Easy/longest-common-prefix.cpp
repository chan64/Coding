#include<bits/stdc++.h>
using namespace std;

string longestCommonPrefix(vector<string>& strs) {
    return ""; 
}

int main()
{
    vector <string> strs = {"flower","flow","flight"};
    cout<<"longestCommonPrefix(strs):"<<longestCommonPrefix(strs);

    return 0;
}